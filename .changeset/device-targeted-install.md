---
"@oniroproject/core": minor
"@oniroproject/oniro-app": minor
---

Add device targeting to app installation. `installApp` now accepts `deviceSerial`, and `oniro-app app install` exposes it as `--device <serial>`.
