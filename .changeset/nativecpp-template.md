---
"@oniroproject/oniro-app": minor
---

Add a `NativeCpp` project template — an ArkTS + Native C++ (N-API) starter with a CMake-built native library. Selectable via `oniro-app create --template NativeCpp` and listed by `oniro-app templates list`.
